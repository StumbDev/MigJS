export function MigJS() {
    Main();
}

import chalk from "chalk";
import { command, program } from "commander";
import { VoilaScript } from "voilascript";
var Entry = Entry()
const voila = new VoilaScript();
const program = new command();
const code = "./main.voi";
voila.execute(code);

export class Main {
    // library entry
    if (Entry = 'mig(:= $var)') {
       // make that part ihihihi
    };

    if (Entry = "$var") {
        this.if(!whitch == 'module'); {
            shell.exec('npx tsc --init');
            shell.exec('npx tsc --build');
        }
    }
    Done();
}

export module MigJSLib{};